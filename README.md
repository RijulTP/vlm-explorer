# VLM Explorer

A local web app that visualizes how CLIP connects images and text — step by step, showing every intermediate output.

## Quick Start

```bash
make install      # create venv + install dependencies
make download     # download CLIP model (~580MB, resumable)
make run          # start server at http://localhost:8000
```
